apiVersion: v1
kind: Service
metadata:
  name: {{ include "checkvatid.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "checkvatid.selectorLabels" . | nindent 4 }}
