apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "checkvatid.fullname" . }}-test-connection"
  labels:
{{ include "checkvatid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "checkvatid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
